<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Perfil/Twitter</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous" />
  <script src="https://kit.fontawesome.com/85d4ade37a.js" crossorigin="anonymous"></script>
</head>

<body>
  <div class="container">
    <div class="row d-flex mt-1 justify-content-between">
      <div class="col-3 bg-primary"></div>
      <div class="col-6">
        <div class="d-flex ">
          <div class="d-flex justify-content-center align-items-center me-4"><a href=""><img
                src="/img/arrow-left-solid.svg" alt="arrowBack" style="width:20px ;"></a></i></div>
          <div class="d-flex flex-column">
            <div class=" fw-bold"><%= user.firstname %> <%= user.lastname %></div>
            <div class=""> @<%= user.username %></div>
          </div>
          <div>
            <div>aca va el background-img</div>
            <div>
              <div>
                <div>foto</div>
                <div>
                  <button class="btn btn-ligth">Editar perfil</button>
                </div>
              </div>
              <div></div>
              <div></div>
              <div>
                <div>Siguiendo</div>
                <div>Seguidores</div>
              </div>
            </div>
          </div>

        </div>
        <br /><br />
        <%= user.description %> <% for(let i = 0; i < tweets.length; i++) {%> <%= tweets[i].text %>
        <form action="/user/mainProfile/<%= tweets[i].id %>?_method=DELETE " method="post">
          <button type="submit">Borrar</button>
        </form>
        <% } %>
        <a href="/user/mainProfile/<%= user.id %>/followers">Followers</a>
        <a href="/user/mainProfile/<%= user.id %>/following">Following</a>
        <a href="/user/mainProfile/<%= user.id %>/edit">Edit Profile</a>
      </div>
      <div class="col-3 bg-secondary"></div>

      <%-// include("partials/navbar") %>

      <!-- <%//- include("partials/footer") %> -->
    </div>
  </div>

</body>

</html>